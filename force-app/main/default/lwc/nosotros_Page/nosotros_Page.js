import { LightningElement } from 'lwc';
import myResource from '@salesforce/resourceUrl/Buildex_images';


export default class Nosotros_Page extends LightningElement {
    capture = myResource + '/Capture.PNG'
    capture2 = myResource + '/Capture2.PNG'

}